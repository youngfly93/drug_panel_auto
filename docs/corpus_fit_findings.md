# 语料 Template-Fit 发现报告

首次用 `scripts/template_fit_analyzer.py` 对本地历史报告语料(脱敏聚合,无 PII)跑 template-fit 分析的结果。回答:**各癌种 family 与 CRC358 golden 模板的契合度,以及接入新 panel 时该复用还是重写。**

- 分析日期:2026-05-29
- Golden baseline:`panels/crc_358_msi/templates/crc_358_msi_golden_template_v0.docx`
- Analyzer:bigram 骨架覆盖率版本(见 `template_fit_methodology.md`,修正变量化偏差后)
- 语料:本地 `各癌种基因报告近年汇总/`(git-ignored,1563 份 docx,15 个癌种)
- 本次覆盖:肠癌 / 肺癌 / 子宫内膜癌 三个最大 family(共 1293 份)

> 隐私:本文件只记录聚合指标(fit 分数、章节命中率、骨架覆盖率)。不含任何病人姓名、样本号、报告原文片段。原始语料与 per-report JSON 留在本地 `tmp/`,不提交。

## 三方对比

| Family | n | 分类 | fit p25/中位/p75 | 结论 |
|---|---:|---|---|---|
| **肠癌** | 603 | **sibling** | 93 / 96 / 96 % | CRC358 golden 是肠癌系列的合适 base,可直接复用 |
| **肺癌** | 487 | **stranger** | 35 / 36 / 38 % | 需要独立的肺癌模板家族,不能套 CRC358 |
| **子宫内膜癌** | 203 | **stranger** | 44 / 44 / 44 % | 需要独立模板,但骨架重合略多于肺癌 |

这与 `panel_report_inventory_audit.md` 的定性判断完全一致:CRC 系列复用 CRC358,肺癌/妇科肿瘤需要新模板家族。

## 章节级解读(接入新 panel 时的设计指引)

### 肺癌(stranger,n=487)
- ✅ **可直接复用**:`检测结果说明`(骨架覆盖 87%)
- 🟡 **骨架部分复用**:`报告导读` 66%、`致患者信`/`阅读说明`/`免疫治疗` 出现时覆盖 89-100%(部分肺癌报告复用了同款固定文案)
- 🔴 **必须重写**:`常见问题` 0%、`检测结果小结` 0%、`基因检测列表` 0%、`靶向药物相关`(肺癌靶向药物体系不同)
- 备注:肺癌目录混了多个产品族(Lung13/20/62+PD/158/329 等),整体 36% 是混合结果;按产品细分后,小靶向 panel 和 PD-L1 panel 的 fit 会进一步分化。

### 子宫内膜癌(stranger,n=203)
- ✅ **可直接复用**:`检测结果说明`(覆盖 92%)、`基因变异解析` 64%、`报告导读` 57%
- 🔴 **完全缺失(命中率 0%)**:`阅读说明`、`常见问题`、`致患者信`、`免疫治疗`、`靶向药物相关`、`诊疗知识`、`信号通路`
- 备注:子宫内膜有自己的"分子分型"体系,约一半 CRC358 章节不存在。需新设计分子分型 + 遗传风险章节。

### 肠癌(sibling,n=603)
- 几乎所有章节高覆盖(整体 96%),验证 golden 模板设计正确。
- ⚠️ 注意:603 份里约 9% fit 偏低,是肠癌目录混入的小 panel(CRC20/35)或老版本报告 —— template-fit 一眼暴露了"同目录不同质"。

## 方法论局限(本次跑暴露,务必知悉)

1. **`novel_sections` 永远为空**:analyzer 只识别 CRC358 的 13 个已知章节标题,所以"肺癌 PD-L1 章节""子宫内膜分子分型章节"这些 CRC358 没有的章节,**不会被识别为新增**,只是静默不匹配。要回答"新 family 缺什么/多什么章节",需要后续的**无监督章节挖掘**(Layer 2,尚未实现)。
2. **分数是相对量,不是绝对真值**:bigram 覆盖率修正了变量化偏差(肠癌 79%→96%),但 stranger 的绝对分数(肺癌 36%)仍受"混合产品族"影响。**用于相对排序和分类可靠;用于精确工作量估算需结合人工 review。**
3. **目录 ≠ 产品族**:一个癌种目录(如肺癌)混了多个 panel 产品,整体 fit 是加权混合。精细化分析需要先按产品名/文件名前缀细分子目录再跑。

## 下一步建议

1. **优先接入肠癌系列其余 panel**(CRC166/208/733/1350 等):sibling,直接复用 CRC358 骨架,最快。
2. **肺癌按产品细分后再跑**:把 487 份按 Lung13/Lung+PD 等产品分组,分别分析,得到每个产品的精确 design brief。
3. **实现 Layer 2 无监督章节挖掘**:回答 stranger family 的"独有章节"问题,补上 `novel_sections` 的盲区。
