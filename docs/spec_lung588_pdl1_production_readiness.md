# 肺癌588基因 + PD-L1 自动化报告生产化规格

状态：`IN_PROGRESS`
产品 ID：`lung_588_pdl1`
基线提交：`9d2280e1f25391f917b24f72cdfb28e7b0815fd5`
工作分支：`codex/lung588-production-readiness`
产品决策确认日期：`2026-07-23`

## 1. 决策与边界

本轮上线对象为独立产品 `lung_588_pdl1`，不是现有草案
`lung_329_pdl1` 的改名版本。

- 保留 `lung_329_pdl1` 为未上线草案，不修改其 329 基因覆盖分母。
- 从 CRC 与肺癌329工作中复用通用映射、渲染、QA 和发布能力。
- 肺癌588的基因分母、筛选规则、药物规则、知识覆盖、模板和 UAT
  全部独立治理。
- 真实 Excel、历史 Word、患者信息、签名和生成报告只作为本地受控输入，
  不进入 Git；仓库只保存脱敏别名、SHA256、结构化期望和审核状态。
- 在医学规则、真实病例 UAT、Linux 渲染和发布门禁完成前，
  Panel 与默认模板保持 `draft`，生产环境继续禁用。

## 2. 已冻结输入与参考身份

### 2.1 真实输入

三份输入均有 12 个可见工作表：

`QC`、`Variations`、`Hereditary_tumor`、`CtDrug`、`Hotspot`、`Cnv`、
`Fusion`、`Msisensor`、`HLA`、`TMB`、`Ct1000`、`Ct15`。

| 脱敏病例 | Excel SHA256 | 用途 |
|---|---|---|
| CASE-LUNG-A | `267a8cbab4d112ea38660dcb1734bb4fb3a7269f50abed6d83a9bf1262ee5646` | 第二病例与边界覆盖 |
| CASE-LUNG-B | `623c96cee1eb7b16cacb62cababba3b790e82007a00a59d0f159efbe025db000` | 历史终版金标准 |
| CASE-LUNG-C | `7b39431044c4a9298f7663c97a47c4df83b5b1e0875d88a64b3e24c05bfa498a` | 历史终版金标准 |

### 2.2 历史终版

| 脱敏病例 | Word SHA256 | 已确认合同 |
|---|---|---|
| CASE-LUNG-B | `8c9cb41572686445…` | 肺癌588基因+PD-L1；8 个报告变异 |
| CASE-LUNG-C | `4754ededa67eeeef…` | 肺癌588基因+PD-L1；9 个报告变异 |

两份终版都包含同一份 588 基因列表：

`ordered_gene_list_sha256 = f9e6be05c954a4d3df97f031d453fe1f58ea0689290b11de3c173f4a0edf08f1`

历史终版仅作为“报告组历史口径 + 版式参考”，不是无需复核的当前医学真理：

- 文件含调试数字残留和近空白页；
- OOXML 含一个损坏的旧关系目标；
- 指南与部分药物证据存在年代和癌种边界风险；
- 因此只锁定经逐项确认的结构化字段，不做整本字节级复制。

## 3. 当前缺陷索引

| ID | 级别 | 当前证据 | 根因 | 目标状态 |
|---|---|---|---|---|
| L588-001 | P0 | 三份肺癌 Excel 在生产基线会被识别成 `crc_358_msi` | 未匹配患者仍继承 `patient_info.yaml:project_info`，Bridge 又把该全局值用于项目识别 | 未知样本不得继承全局项目；文件名无可信项目词时返回未识别并要求人工选择 |
| L588-002 | P0 | 仓库只有 `lung_329_pdl1` 草案；历史终版明确为 n=588 | 产品身份、基因分母和模板合同混淆 | 新建独立 `lung_588_pdl1`，588 基因清单哈希受门禁保护 |
| L588-003 | P0 | 三份 Excel 均不含 PD-L1/TPS/CPS | PD-L1 是独立 IHC 结果，不能从 NGS Excel 推断 | TPS、CPS、结果判定由受控表单输入；缺失或矛盾时阻断 |
| L588-004 | P1 | 强制走肺癌候选链路时，B/C 变异可达 8/8、9/9；生产默认链路不能做到 | `Variations` 为复合表，肺癌需按显式Ⅰ/Ⅱ/Ⅲ类及合法 HGVS 过滤；当前生产兼容增强器只认 CRC 小 Panel 列 | Panel 级筛选合同 + 两个真实金标准 exact diff |
| L588-005 | P0 | 当前肺癌候选靶向药物计数为 0；历史终版有大量药物条目 | 肺癌药物规则尚未形成经审核的癌种、变异、方向和证据边界 | 规则默认关闭；逐事件迁移、结构化来源、二审状态和反例测试后才开启 |
| L588-006 | P1 | 历史终版含免疫正/负/超进展基因结论 | 旧逻辑可能把基因级关联泛化到任意变异 | 免疫规则按基因+变异类型+癌种+证据边界治理，未知条目大声报警 |
| L588-007 | P1 | Excel 含 `CtDrug/Ct1000/Ct15`，历史终版有化疗 PGx 章节 | 表结构复杂且尚无肺588真实病例映射合同 | 单独建立 PGx 输入映射与药物表 gold diff；未完成前章节显式阻断 |
| L588-008 | P1 | 历史终版有调试残留、损坏关系和近空白页 | 旧 Word 被当作手工成品维护 | 从终版样式变量化、清 PII/调试项，Linux LibreOffice 视觉 QA |
| L588-009 | P1 | 现有肺329候选只覆盖329分母，且大量通用 fallback | 肺588知识深度和医学审核状态尚未建立 | 对588基因分别统计非空覆盖、肺癌特异覆盖、来源覆盖和二审覆盖 |
| L588-010 | P1 | 文件名只有样本编号，无法可靠自动识别产品 | 当前 UI/批量链路过度依赖自动识别 | 单份与批量都支持明确选择 `lung_588_pdl1`，任务快照记录选择来源 |
| L588-011 | P1 | 肺588尚无正式 UAT、冻结审计和生产部署记录 | 新产品发布链未建立 | 至少10份脱敏真实病例 UAT、通过率≥90%、P0=0，随后部署并活实例验收 |
| L588-012 | P0 | NGS Excel 不含 PD-L1，而现有批量表单只支持一组共享临床字段 | 若开放批量，会把同一 TPS/CPS/判定串用到多个患者 | 在逐文件临床载荷实现前阻断肺588批量；单份病例逐项校验 |
| L588-013 | P1 | 工程探针中第三部分调用通用基础库并出现跨癌种历史文献；588 基因表尾部形成 14 基因孤页 | 未审核 Panel 默认回退共享知识；329 模板行高直接扩展到588 | Panel 级 Part3 门禁默认关闭；独立知识二审后才开启；压缩基因表行高并做 LibreOffice 页级 QA |
| L588-014 | P0 | 329 脚手架在 PD-L1 页内嵌一张固定显微图；肺588历史输入不含图像，现有可追溯文本也未给出抗体克隆和检测平台 | 旧模板把病例级 IHC 资产当成静态版式资源复用，动态替换 TPS/CPS 时会造成跨病例图像污染 | 工程模板彻底移除静态显微图及其 DOCX 媒体关系；未提供逐病例可追溯图像时显示明确说明；进入 pilot 前由报告组确认“逐病例图像”或“正式无图”合同，并补齐抗体克隆、检测平台及来源 |
| L588-015 | P0 | 已筛出的4条候选均依赖肺癌病理类型、疾病范围、既往系统治疗或伴随诊断状态，但原表单不采集这些结构化字段 | 只有基因/位点和自由文本诊断，无法安全判断药物标签的分期、线次和检测条件 | 肺588专属表单新增4个受控上下文字段；运行时规则加载器按逐候选允许值失败关闭；工程草案暂不强制，任何缺失/待确认/超范围值都保持候选隐藏 |

## 4. 金标准合同

### 4.1 CASE-LUNG-B

- 报告变异必须精确为 8 条：
  `TP53 p.H193L`、`ATM c.1236-2A>T`、`TSC1 p.R692*`、
  `MSH3 p.Q969*`、`BRCA2 p.R2336H`、`MLH1 c.790+1G>A`、
  `PMS2 p.R20Q`、`BRAF p.D594G`。
- TMB：`7.5 mutations/Mb`，`TMB-L`。
- MSI：`MSS`。
- PD-L1：TPS `50%`、CPS `52`、阳性/高表达。
- 靶向、免疫、PGx 条目先作为历史候选合同，医学规则复核后再锁 exact diff。

### 4.2 CASE-LUNG-C

- 报告变异必须精确为 9 条：
  `BRAF p.V600E`、`SMAD4 p.A464Rfs*27`、`PTEN c.802-2A>T`、
  `ERBB2 p.G660D`、`ATM c.1236-2A>T`、`TP53 p.G245D`、
  `TSC2 p.Q883*`、`BRIP1 p.W714*`、`PIK3CA p.A1066V`。
- TMB：`6.3 mutations/Mb`，`TMB-L`。
- MSI：`MSS`。
- PD-L1：TPS `5%`、CPS `6`、阳性/低表达。
- 靶向、免疫、PGx 条目先作为历史候选合同，医学规则复核后再锁 exact diff。

### 4.3 历史语义盘点与候选边界

两份历史终版的源文件保持不变。工具仅在 `.work/` 生成修复副本，
各移除 1 个指向 `../NULL` 的失效图片关系及对应孤儿绘图节点；
修复前后源文件 SHA256 一致，副本均可由 `python-docx` 重新打开。

对修复副本执行脱敏语义抽取后得到：

| 类别 | 数量 | 当前处置 |
|---|---:|---|
| 历史靶向药物显示记录 | 169 | 仅作为旧显示合同，不直接迁移 |
| 历史精确靶向事件 | 15 | 按事件逐条复核 |
| 历史 A 级显示记录 | 9 | 不继承旧等级，重新核对癌种、位点和适应证 |
| 免疫基因观察 | 8 | 基因级泛化风险，运行时关闭 |
| 化疗 PGx 观察 | 208 | 尚无合格处方合同，运行时关闭 |

当前仅保留 4 条证据边界较明确的**候选**：

- `BRAF c.1799T>A / p.V600E`：
  达拉非尼+曲美替尼、康奈非尼+贝美替尼；
- `ERBB2 c.1979G>A / p.G660D`：
  德曲妥珠单抗、瑞康曲妥珠单抗。

候选记录位于 `rules/medical_candidates.yaml`，均包含精确 c./p. 位点、
肺癌类型与分期/既往治疗/检测条件、结构化来源和审核状态。该文件不是
运行时药物规则源，4 条候选均为 `runtime_eligible: false`、
`report_text_allowed: false`；其余 165 条历史记录明确不迁移。

特别保留以下反例门禁：

- `BRAF p.D594G` 不得继承 `BRAF V600E` 组合治疗规则；
- `ERBB2 p.G660D` 不得因基因相同自动继承只适用于激酶结构域的规则；
- 宗艾替尼、恩美曲妥珠单抗和吡咯替尼暂不进入该事件的患者级规则；
- 任意旧 C/D 级、篮式试验、跨癌种或基因级文本均不得自动进入报告。

## 5. 分阶段实施

### Phase A：产品身份与输入门禁

1. 修复全局 `project_info` 污染项目识别。
2. 新建 `lung_588_pdl1` 草案包、别名、588 基因清单和输入合同。
3. 单份手工选择优先于自动识别，并在任务快照中留痕。
4. PD-L1 三字段做范围、一致性和缺失阻断。
5. 批量在支持逐文件 PD-L1 载荷前显式阻断，不允许共享一组值。
6. 模板不得携带静态 PD-L1 病例图像；图像、抗体克隆和检测平台必须逐病例可追溯，
   或由报告组签发正式无图合同。

### Phase B：结构化结果与金标准

1. 将肺癌显式Ⅰ/Ⅱ/Ⅲ类筛选做成 Panel 规则。
2. 固化 CASE-LUNG-B/C 的变异、TMB、MSI、PD-L1 exact diff。
3. 补 CNV、融合、HLA、阴性结果和复合表边界测试。

### Phase C：知识和医学规则

1. 先盘点两个金标准实际命中的精确事件，并将可追溯条目保存为
   非运行时候选。
2. 每条规则必须包含癌种、事件类型、方向、证据等级、来源和审核状态。
3. 未审核条目不得进入运行规则；知识缺口必须进入 QA。
4. 分开统计588基因基础覆盖、肺癌特异覆盖和二审覆盖。
5. 候选只有在报告组逐事件二审、当前标签/指南版本核对、正反例测试和
   病例 UAT 后，才能另行迁移至 `rules/drugs.yaml`；候选文件本身永不
   作为运行时来源。
6. 药物候选还必须按事件校验肺癌病理类型、疾病范围、既往系统治疗和
   伴随诊断适配状态；缺失、`未明确` 或 `待确认` 时失败关闭。

### Phase D：Word 与视觉 QA

1. 从历史终版保留样式，去除病例值、调试残留、损坏关系和旧生成字段。
2. 表格使用结构化循环，Part3 使用动态 marker。
3. PD-L1 页面验证“无静态病例图像”；若展示图像，必须验证该病例资源身份。
4. 在 Linux LibreOffice 下检查目录、分页、空白页、表格宽度、签名和页码。

### Phase E：UAT 与部署

1. 至少10份脱敏真实肺588病例，且包含两个历史金标准。
2. 总通过率不低于90%；P0、串病例、错 Panel、关键结果错误均为0。
3. 冻结 commit，自审固定身份，运行发布总闸。
4. 以 immutable release 部署 iyun129，验证健康检查、单份、批量、
   下载、任务恢复和版本回滚。

## 6. 发布状态定义

- `ENGINEERING_DRAFT`：包可加载，规则默认关闭，不允许交付。
- `VERIFIED_CANDIDATE`：结构和两个金标准通过，但医学二审/UAT未完成。
- `PROMOTION_READY`：医学二审、UAT、Linux视觉QA和总闸全部通过。
- `PRODUCTION_ACTIVE`：iyun129 已切换到冻结 SHA，活实例验收通过。

在达到 `PROMOTION_READY` 前，不得把肺588描述为“已上线”或“医学知识库已全部完成”。

## 7. 当前工程证据与未决发布项

截至 `2026-07-23`：

- 产品身份、588 有序基因分母、Ⅰ/Ⅱ/Ⅲ类筛选、PD-L1 三字段范围/分层校验、
  批量阻断、Part3 关闭和生产禁用边界已进入工程实现与自动回归。
- 确定性模板输入 SHA256 为
  `bbecf470feedab8ba20c4d84f7e4f8f0e2f0714f8f50818dc0419d72d2767e66`，
  当前肺588模板 SHA256 为
  `653b6750a3c06bfaf757610907c58a1f6632d9bfe7729f572ab409c6f143e94b`。
- CASE-LUNG-B/C 已建立脱敏 exact contract；真实输入及报告文件只在 `.work/`
  和外部受控目录中使用，不进入 Git。
- iyun129 隔离 QA 副本已使用 Linux LibreOffice 7.3.7.2 对 A/B/C 三份
  真实输入完成全页渲染：均为 27 页，机器 QA PASS，空白页、异常低内容页
  和内容串漏均为 0；测试期间生产仍运行 `49bae3d`，未切换 release。
- 静态 PD-L1 显微图已定为禁止资产；候选模板只允许病例级可追溯图像或明确无图说明。
- 历史 Word 失效关系已在不修改源文件的前提下形成可复现修复副本；历史靶向、
  免疫和 PGx 语义已完成脱敏盘点。
- `BRAF V600E` 与 `ERBB2 G660D` 共 4 条证据受限候选已落入独立候选文件，
  但全部保持 `runtime_eligible: false`；肺588运行时药物规则仍关闭。
- 肺588专属临床表单已增加病理类型、疾病范围、既往系统治疗和伴随诊断
  适配状态四个受控字段；工程草案中暂为可选，候选晋级时缺失/不确定值必须
  阻断药物输出。
- 逐候选上下文匹配器已经接入请求级药物规则加载路径；规则缺合同、字段缺失、
  值不确定或超出事件适应证时均转入阻断 selector，不能回退到较宽泛药物行。
  该能力不改变当前 `rules/drugs.yaml` 的 `enabled: false`。
- 588 基因知识深度已经按固定分母形成逐行队列：588/588 有简介，341/588
  有变异解析，32/588 为非通用特异解释，37/588 有固定蛋白/结构域内容；
  仍有247个变异解析缺口、300个通用fallback和551个固定结构域缺口
  （各类缺口有重叠），不得将非空覆盖表述为医学完成。
- CASE-LUNG-A/B/C 的23个唯一事件已将转录本、染色体、外显子、c./p.、
  Ⅰ/Ⅱ/Ⅲ类和VAF纳入脱敏合同；ATM `NM_000051.4:c.1236-2A>T` 在两个
  病例中去重为一个医学审核事件，同时保留两组病例观察。
- P0一审包当前包含28个单元：23个唯一变异解释、4条精确药物候选和1条
  文献—声明错配；Codex一审只完成安全分流，患者可见第三部分和药物结论
  允许数仍为0，二审完成数为0。
- STK11历史文本中的 PMID 25980754 已确认为不支持KRAS/STK11肺癌免疫
  治疗声明；元数据已登记，但语义来源门禁继续以
  `RUNTIME_CITATION_SOURCE_MISMATCH` 阻断。PMID 29773717只作为建议替代
  来源进入待二审队列，未自动晋级为运行知识。
- 报告组逐事件二审、当前标签/指南确认、588 知识深度、抗体克隆/检测平台、
  至少 10 份病例 UAT 尚未完成。当前仅有 3 份真实病例机器预 UAT 全部通过，
  报告组逐病例 UAT 仍为 `0/10`；因此发布状态仍为 `ENGINEERING_DRAFT`，
  不得从生产禁用列表移除。
