schema_version: "1.0"
panel_id: "crc_35_msi"

current_output:
  enabled: true
  source: "golden_candidate"
  required_features:
    total_variants_count: warn
    drug_related_count: warn
    msi_status: warn
  required_sections:
    variant_summary: warn
    gene_list: warn
  require_table_shapes: warn
  privacy_checks:
    report_id: fail
    sample_id: fail
    date: fail

legacy_reference:
  enabled: false
  source_dir_name: "crc_35_msi"
  sample_count: 2
  required_features:
    total_variants_count: warn
    drug_related_count: warn
    msi_status: warn
  required_sections:
    variant_summary: warn
    gene_list: warn
  require_table_shapes: warn
  privacy_checks:
    source_dir: fail
    report_id: fail
    sample_id: fail
    date: fail
