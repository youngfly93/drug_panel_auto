schema_version: "1.0"
panel_id: "lung_329_pdl1"
rule_id: "drugs"
version: "0.4.0"
updated: "2026-07-25"
status: "pilot"
description: "Fail-closed lung329 treatment policy pending event-level medical review."

drug_rules:
  approved_drug_rows_source: "none"
  brand_lookup_source: "config/drug_brands.yaml"
  immunotherapy_brand_list_source: "config/drug_brands.yaml"
  evidence_levels:
    allowed: ["A", "B", "C", "D"]

# Lung-specific drug decisions have not completed review. Fail closed: neither
# global candidate rows, internal CRC rows, CtDrug fallback, nor fixed CRC 2.2
# rows may enter a lung report through the reused compatibility enhancer.
targeted_drug_rules:
  enabled: false
  base_db_enabled: false
  allowed_source_dbs: []
  allow_internal_rows: false
  require_cancer_profile_match: true
  approved_drug_rows_enabled: false

approved_drug_rows: []
